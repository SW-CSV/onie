mount TLV eeprom on i2c-1.

diff --git a/miscutils/sys_eeprom_i2c.c b/miscutils/sys_eeprom_i2c.c
index a8d3836..5ea732b 100644
--- a/miscutils/sys_eeprom_i2c.c
+++ b/miscutils/sys_eeprom_i2c.c
@@ -30,7 +30,7 @@ int read_sys_eeprom(void *eeprom_data, int offset, int len)
         return -1;
      }
 #else
-    if(eeprom_open("/dev/i2c-0", 0x56,
+    if(eeprom_open("/dev/i2c-1", 0x56,
                     2, &e)) {
         printf("ERROR: Cannot open I2C device\n");
         return -1;
@@ -65,7 +65,7 @@ int write_sys_eeprom(void *eeprom_data, int len)
             return -1;
         }
 #else
-        if (eeprom_open("/dev/i2c-0", 0x56,
+        if (eeprom_open("/dev/i2c-1", 0x56,
                         2, &e)) {
             printf("ERROR: Cannot open I2C device\n");
             return -1;
